Automated tests are not implemented in this pass.

Recommended next additions:

- config parsing tests
- depth packet framing/parse tests
- preview colorization tests
- HTTP contract tests for `/health`, `/metadata`, `/settings`, and `/streams`
