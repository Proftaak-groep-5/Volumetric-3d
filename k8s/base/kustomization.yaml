apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: volumetric-3d

resources:
  - configmaps/platform--env-configmap.yaml
  - api/                    # ← Directory, niet het yaml bestand!
  - frontend/               # ← Directory
  - database/               # ← Directory
  - falco/                  # ← Directory